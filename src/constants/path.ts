export const PATH_PAGE = {
    root: "/"
}

export const PATH_AUTH = {
    login: "/login",
    register: "/register",
    forgot: "forgot-pass"
}

export const PATH_BUSINESS = {
    detail: (id: string) => `/homestay/${id}`,
}